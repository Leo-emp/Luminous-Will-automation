# ============================================================
# REQUIRED PYTHON PACKAGES
# Install with: pip install -r requirements.txt
# ============================================================

# --- Video editing and assembly ---
moviepy>=2.0.0          # video compositing, cutting, effects, export

# --- Image processing ---
Pillow>=10.0.0          # caption rendering, logo processing
opencv-python-headless  # video frame extraction, image processing
numpy                   # array operations for color grading

# --- Text-to-Speech (ElevenLabs) ---
elevenlabs>=1.0.0       # voiceover generation with word timestamps
requests                # API calls to ElevenLabs and Pexels

# --- Audio processing ---
pydub>=0.25.0           # voiceover normalization for consistent phone-optimized volume
librosa>=0.11.0         # beat detection for quote reel zoom sync

# --- Environment variables ---
python-dotenv           # loads API keys from .env file

# --- AI Script Generation (Gemini) ---
google-generativeai>=0.8.0  # Gemini API for long-form script generation

# --- Social Media Publishing ---
google-api-python-client>=2.0.0   # YouTube Data API v3
google-auth-oauthlib>=1.0.0       # Google OAuth2 flow
requests-oauthlib>=1.3.0          # OAuth2 for TikTok/Facebook
schedule>=1.2.0                    # Cron-style scheduler

# --- Testing ---
pytest>=7.0.0               # unit testing
